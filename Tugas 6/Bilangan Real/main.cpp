#include <iostream>

using namespace std;

int main()
{
    int x;
    cout << "Bilangan: " << endl;
    cin >> x;
    if (x > 0 && x <= 100 && x % 2 == 0){
        cout << "genap" << endl;
    }
    else if (x % 2 == 1) {
        cout << "ganjil" << endl;
    }else if

    (for (x = 6;x<=6;x++);
    for (x = 6;x<=x*6;x=x+6){
        }
        cout<<endl;
        cout<<"total : "<<total;)
    }

    return 0;
}
